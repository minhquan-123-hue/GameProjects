#include <stdio.h> // #include == same idea with import (use others code)

int main() // int main () == entry ponit & function & return number => return 0; tell OS that finish compile all line of code
{
    // printf == print (write on screen)
    // \n == press enter
    // {} : block , ; : end of line
    printf("Hello, world!\n");
    printf("i love program and physic to be able to good at it , practice and do real project , there's no shortcut to it, let's go\n");
    return 0;
}